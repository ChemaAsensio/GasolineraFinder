import { CleanCompanyNamePipe } from './clean-company-name-pipe';

describe('CleanCompanyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCompanyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
